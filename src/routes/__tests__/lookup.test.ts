import { describe, it } from "vitest";

describe("POST /lookup", () => {
  it.todo("returns 400 for missing request body");
  it.todo("returns 404 for unknown card identifier");
  it.todo("returns pricing data for a valid card");
});
